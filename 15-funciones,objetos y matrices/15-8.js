numbers=[]

numbers.push("One")
numbers.push("Two")
numbers.push("Three")

for (let index = 0; index < numbers.length; index++) {
    const element = numbers[index];
    console.log(element);
    document.write(element)
}

